language: ruby
services:
  - docker
before_install:
  - gem install bundler
  - gem install trigger_build
after_success:
  - trigger_build MYOB-Technology http_stub_example_producer
cache: bundler
notifications:
  email:
    on_success: change
    on_failure: always
